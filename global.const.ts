/**
 * BCP-47 locale codes supported by this site.
 * Used to validate locale-specific routes and select message bundles.
 */
const SUPPORTED_LOCALES = ['en'] as const;

export { SUPPORTED_LOCALES };
